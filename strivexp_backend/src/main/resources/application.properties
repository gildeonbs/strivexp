# =====================================================
# Application Info
# =====================================================
spring.application.name=StriveXP

# =====================================================
# Server Configuration
# =====================================================
server.port=8080

# =====================================================
# PostgreSQL DataSource
# =====================================================
spring.datasource.url=jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:strivexp_db}?stringtype=unspecified
spring.datasource.username=${POSTGRES_USER:strivexp_user}
spring.datasource.password=${POSTGRES_PASSWORD:strivexp_pass}
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection Pool (HikariCP)
spring.datasource.hikari.pool-name=StriveXPHikariCP
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.leak-detection-threshold=20000

# =====================================================
# Hibernate / JPA
# =====================================================
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.hibernate.ddl-auto=validate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# =====================================================
# Flyway (Database Migrations)
# =====================================================
spring.flyway.enabled=false
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=7
spring.flyway.locations=classpath:db/migration
spring.flyway.schemas=public
#spring.flyway.user=${POSTGRES_USER:strivexp_user}
#spring.flyway.password=${POSTGRES_PASSWORD:strivexp_pass}

# =====================================================
# SendGrid / SMTP Configuration
# =====================================================
#spring.mail.host=${STRIVEXP_MAIL_SERVER}
#spring.mail.port=587
#spring.mail.username=${STRIVEXP_MAIL_ADDRESS}
#spring.mail.password=${STRIVEXP_MAIL_PASSWORD}
#spring.mail.properties.mail.smtp.auth=true
#spring.mail.properties.mail.smtp.starttls.enable=true

# =====================================================
# Resend Configuration
# =====================================================
#resend.api.key=${RESEND_API_KEY}

# =====================================================
# Application Config
# =====================================================
#app.frontend.url=https://placeholder-url-random-name.trycloudflare.com
#app.frontend.url=http://localhost:8080
#app.frontend.url=https://strivexp.onrender.com

# Dynamic Frontend URL for emails
# Default to localhost if not set (safe for local dev)
#app.frontend.url=${APP_FRONTEND_URL:http://localhost:8080}

#app.email.from=${STRIVEXP_MAIL_ADDRESS}
# IMPORTANT: With Resend, the 'from' email MUST be a domain you verified (e.g., 'onboarding@resend.dev' for testing)
#app.email.from=${RESEND_FROM_EMAIL:onboarding@resend.dev}

# =====================================================
# Google Gmail API Configuration
# =====================================================
# These values come from your Google Cloud Console and OAuth Playground
google.client.id=${GOOGLE_CLIENT_ID}
google.client.secret=${GOOGLE_CLIENT_SECRET}
google.refresh.token=${GOOGLE_REFRESH_TOKEN}
google.user.email=${GOOGLE_USER_EMAIL}

# =====================================================
# Application Config NEW
# =====================================================
app.frontend.url=${APP_FRONTEND_URL:http://localhost:8080}
app.email.from=${GOOGLE_USER_EMAIL}

# =====================================================
# JWT SECRET
# =====================================================
jwt.secret=${JWT_SECRET}